{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
<h1 class="mb-4">📊 Dashboard Administrateur</h1>

<p class="text-muted mb-4">
    Supervision globale des projets et des tâches
</p>

{# ===== STATISTIQUES ===== #}
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card text-bg-primary">
            <div class="card-body text-center">
                <h6 class="card-title">Projets</h6>
                <p class="display-6">{{ totalProjects }}</p>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-bg-success">
            <div class="card-body text-center">
                <h6 class="card-title">Tâches</h6>
                <p class="display-6">{{ totalTasks }}</p>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-bg-warning">
            <div class="card-body text-center">
                <h6 class="card-title">À faire</h6>
                <p class="display-6">{{ todoTasks }}</p>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-bg-info">
            <div class="card-body text-center">
                <h6 class="card-title">Terminées</h6>
                <p class="display-6">{{ doneTasks }}</p>
            </div>
        </div>
    </div>
</div>

{# ===== LISTE DES PROJETS ===== #}
<h3 class="mb-3">📁 Projets</h3>

<table class="table table-striped">
    <thead class="table-dark">
        <tr>
            <th>Projet</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
    {% for project in projects %}
        <tr>
            <td>{{ project.name }}</td>
            <td>{{ project.description }}</td>
        </tr>
    {% else %}
        <tr>
            <td colspan="2" class="text-center text-muted">
                Aucun projet trouvé
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
