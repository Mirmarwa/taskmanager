<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}TaskManager{% endblock %}</title>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🗂️</text></svg>">

{# Bootstrap CSS #}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

{% block stylesheets %}
    <!-- Your custom CSS -->
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}


</head>

<body>

{# ===== NAVBAR ===== #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">

    <div class="container-fluid">
    <button class="btn btn-outline-light me-2"
        type="button"
        data-bs-toggle="offcanvas"
        data-bs-target="#sidebarMenu">
    ☰
</button>


        <a class="navbar-brand" href="#">TaskManager</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav me-auto">

                {% if app.user %}
                    {# USER STANDARD #}
                    {% if is_granted('ROLE_USER') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard') }}">Dashboard</a>
                        </li>
                    {% endif %}

                    {# ADMIN #}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard_admin') }}">Dashboard Admin</a>
                        </li>
                    {% endif %}

                    {# DIRECTOR #}
                    {% if is_granted('ROLE_DIRECTOR') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard_director') }}">Dashboard Directeur</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                             <a class="nav-link" href="{{ path('app_workspace_index') }}">Workspaces</a>
                    </li>

                     
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_project_index') }}">Projects</a>
                    </li>


                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_task_index') }}">Tasks</a>
                    </li>
                    <li class="nav-item">
    <a class="nav-link" href="{{ path('app_task_kanban') }}">Kanban</a>
</li>
<li class="nav-item">
    <a class="nav-link position-relative" href="{{ path('app_notifications') }}">
        🔔 Notifications

        {% set unread = app.user.notifications|filter(n => not n.isRead)|length %}
        {% if unread > 0 %}
            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                {{ unread }}
            </span>
        {% endif %}
    </a>
</li>


                {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <span class="badge rounded-pill bg-light text-dark me-3 px-3 py-2 mt-1">
    {{ app.user.email }}
</span>




                    </li>

                    <li class="nav-item">
                        <a class="nav-link text-danger" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                    </li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>
<div class="offcanvas offcanvas-start" tabindex="-1" id="sidebarMenu">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title">Menu</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>

    <div class="offcanvas-body">
        <ul class="list-group list-group-flush">

            <a class="list-group-item" href="{{ path('app_workspace_index') }}">Workspaces</a>
            <a class="list-group-item" href="{{ path('app_project_index') }}">Projects</a>
            <a class="list-group-item" href="{{ path('app_task_index') }}">Tasks</a>
            <a class="list-group-item" href="{{ path('app_task_kanban') }}">Kanban</a>

        </ul>
    </div>
</div>


{# ===== PAGE CONTENT ===== #}
<div class="container mt-4">

    {# ===== FLASH MESSAGES ===== #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} mt-2">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
{% block body %}{% endblock %}
</div>

{# Bootstrap JS #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

{# Chart.js DOIT être chargé AVANT les scripts des pages #}
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

{# Your custom JS (animations, UI effects) #}
<script src="{{ asset('js/app.js') }}"></script>

{% block javascripts %}
    <script src="{{ asset('js/app.js') }}"></script>
{% endblock %}


</body>
</html>

